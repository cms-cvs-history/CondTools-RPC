process DBTest = {
# empty input service, fire events
        source = EmptySource {untracked int32 maxEvents = 1}

	es_source = PoolDBESSource { 
                      VPSet toGet = {
                        { string record = "RPCReadOutMappingRcd"
                          string tag = "RPCReadOutMapping_v1"
                        } 
                      }
                      bool loadAll = true
                      string timetype = "runnumber" 
                      string connect = "sqlite_file:cabling.db"
                      untracked string catalog = "file:cablingCatalog.xml"
                    }

      module readstruct =  RPCReadOutMapAnalyzer{}

        path p1 = {readstruct}      
}

