process ProcessOne = {
 service = MessageLogger {
    untracked vstring destinations = {"cout"}
    untracked PSet cout = {
          untracked  PSet default = { untracked int32 limit = 0 }
    }
 }

//Subproject specific modules
     source = EmptyIOVSource {
         string timetype = "runnumber"
         untracked uint32 firstRun = 1
         untracked uint32 lastRun = 1
         uint32 interval = 1
       }


//PopCon config
include "CondCore/DBCommon/data/CondDBCommon.cfi"
// eg to write the objects in oracle

replace CondDBCommon.connect = "oracle://cms_orcon_prod/CMS_COND_20X_RPC"
replace CondDBCommon.DBParameters.authenticationPath = "/nfshome0/xiezhen/conddb"
replace CondDBCommon.DBParameters.messageLevel=1
// Database output service
    service = PoolDBOutputService{
	using CondDBCommon
	VPSet toPut = { { string record = "RPCdbDataRcd" string tag = "RPCcond_1" } }
        //official log db is to be defined by policy. 
        // should beCMS_COND_GENERAL schema in official db
	//untracked string logconnect = "sqlite_file:log.db"
	untracked string logconnect = "oracle://omds/CMS_POP_CON"
   }


//Common parameters to all subprojects
module Test1 = RPCPopConAnalyzer {
   string record = "RPCdbDataRcd"
   untracked bool loggingOn = True
   bool SinceAppendMode = true
   PSet Source ={

   // Set since = 0 for automatic mode from file
   untracked int32 since = 0
   untracked string logpath = "/tmp/dpagano/RPCCond.log"
  
   untracked string host = "oracle://cms_omds_lb/CMS_RPC_PVSS_COND"
   untracked string user = "CMS_RPC_PVSS_COND"
   untracked string passw = "******"
   untracked string Ohost = "oracle://cms_orcon_prod/CMS_COND_20X_RPC"
   untracked string Ouser = "CMS_COND_20X_RPC"
   untracked string Opassw = "******"

   }
}

 path p = {Test1}
}

