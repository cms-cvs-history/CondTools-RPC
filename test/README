1. To write RPCReadOutMapping into PoolDB (currently sqlite_file) from 
   OMDS execute:

$ RPCReadOutMapping-O2O oracms.cern.ch OMDS RPC_CONFIGURATION ***** 10121

2. To test PoolDB execute:

$ cmsRun -p read.cfg
